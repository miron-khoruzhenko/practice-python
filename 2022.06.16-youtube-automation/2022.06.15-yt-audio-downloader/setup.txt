pip install yt-dlp

если есть ошибка ffmpeg смотри папку пакеты


renamer.py убирает ' - Topic' и если имя композитора одинаково в квадратных скобках
и после них то убирает то что после них.

Так же создает лог фаил с обработаной музыкой.